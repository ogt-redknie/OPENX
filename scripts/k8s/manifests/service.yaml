apiVersion: v1
kind: Service
metadata:
  name: opnex
  labels:
    app: opnex
spec:
  type: ClusterIP
  selector:
    app: opnex
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
