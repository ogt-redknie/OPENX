apiVersion: v1
kind: ConfigMap
metadata:
  name: opnex-config
  labels:
    app: opnex
data:
  opnex.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.opnex/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "OPNEX Assistant",
            "workspace": "~/.opnex/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # OPNEX Assistant

    You are a helpful AI assistant running in Kubernetes.
